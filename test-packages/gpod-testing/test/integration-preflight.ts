/**
 * Preloaded by `bun test --preload` for gpod-testing's own integration tests.
 * Only needs gpod-tool (this package wraps it).
 */
import { requireGpodTool } from '../src/preflight';

requireGpodTool();
