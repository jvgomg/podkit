{
  "name": "@podkit/device-testing-daemon",
  "version": "0.0.1",
  "private": true,
  "description": "FunctionFS userspace daemon: synthesises iPod-shaped USB devices on Linux dummy_hcd for VM tests (ADR-016). Compiled to a single self-contained binary via `bun build --compile`; consumed by @podkit/device-testing's lima-test-vm runner.",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "bash scripts/build.sh",
    "build:linux-x64": "bash scripts/build.sh linux-x64",
    "build:linux-arm64": "bash scripts/build.sh linux-arm64",
    "test": "bun test src/__tests__",
    "test:unit": "bun test src/__tests__ --pass-with-no-tests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@podkit/device-testing": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.9.0"
  }
}
