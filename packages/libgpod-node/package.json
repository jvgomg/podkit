{
  "name": "@podkit/libgpod-node",
  "version": "0.0.1",
  "description": "Native Node.js bindings for libgpod",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "prebuilds",
    "native",
    "binding.gyp"
  ],
  "scripts": {
    "build": "[ \"$SKIP_NATIVE_BUILD\" = \"1\" ] && bun run build:ts || (bun run build:native && bun run build:ts)",
    "build:native": "node-gyp rebuild",
    "build:ts": "bun build src/index.ts --outdir dist --target node && bun run build:types",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly --outDir dist",
    "prebuildify": "prebuildify --napi --strip",
    "clean": "rm -rf dist build",
    "test": "bun test",
    "test:unit": "FILES=$(find src -name '*.test.ts' ! -name '*.integration.test.ts' 2>/dev/null); [ -n \"$FILES\" ] && bun test $FILES || echo 'No unit tests found'",
    "test:integration": "FILES=$(find src -name '*.integration.test.ts' 2>/dev/null); [ -n \"$FILES\" ] && bun test $FILES || echo 'No integration tests found'",
    "typecheck": "tsc --noEmit",
    "rebuild": "node-gyp rebuild"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "devDependencies": {
    "@podkit/gpod-testing": "workspace:*",
    "@types/bun": "latest",
    "node-gyp": "^11.0.0",
    "prebuildify": "^6.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "gypfile": false
}
