{
  "name": "@podkit/docker",
  "private": true,
  "version": "0.2.3",
  "description": "Docker image for podkit — syncs music collections to iPod devices",
  "scripts": {
    "test": "bats test/",
    "test:smoke": "bash test/image-smoke.sh"
  },
  "dependencies": {
    "@podkit/daemon": "workspace:*",
    "podkit": "workspace:*"
  },
  "devDependencies": {
    "bats": "^1.11.1"
  }
}
