{
  "name": "@podkit/ipod-db",
  "version": "0.0.1",
  "description": "Pure TypeScript iTunesDB parser for iPod devices",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node && bun run build:types",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly --outDir dist",
    "generate-fixtures": "bun run fixtures/generate.ts",
    "test": "bun test",
    "test:unit": "FILES=$(find src -name '*.test.ts' ! -name '*.integration.test.ts' 2>/dev/null); if [ -n \"$FILES\" ]; then bun test $FILES; else echo 'No unit tests found'; fi",
    "test:integration": "FILES=$(find src -name '*.integration.test.ts' 2>/dev/null); if [ -n \"$FILES\" ]; then bun test $FILES; else echo 'No integration tests found'; fi",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@podkit/gpod-testing": "workspace:*",
    "@podkit/libgpod-node": "workspace:*",
    "@types/bun": "latest"
  }
}
