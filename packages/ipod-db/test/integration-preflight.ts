/**
 * Preloaded by `bun test --preload` for ipod-db integration tests.
 * Needs gpod-tool (used to generate test fixtures via the @podkit/gpod-testing
 * fixture pipeline).
 */
import { requireGpodTool } from '@podkit/gpod-testing';

requireGpodTool();
