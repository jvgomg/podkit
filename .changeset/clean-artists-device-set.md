---
"podkit": patch
---

Add `--clean-artists` / `--no-clean-artists` / `--clear-clean-artists` options to `podkit device set`

The clean artists transform can now be toggled per-device from the CLI instead of requiring manual config file edits.
