---
"podkit": minor
---

Add official Docker image for running podkit in containers. The image is based on Alpine Linux and supports both `linux/amd64` and `linux/arm64` architectures. Published to GitHub Container Registry on each release.

Also adds musl-compatible Linux binaries to release assets for users on Alpine and other musl-based distributions.
